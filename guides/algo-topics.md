---
name: algo-topics
type: data
description: >
  Algorithm 튜터 Mode B 주제 목록. 트랙(DS·AIE·공통) × 레벨(1~5) × 그룹별 세부 토픽.
  tutoring_topic_picker.py --tutor algo --track {DS|AIE|공통} 이 이 파일을 파싱한다.
  헤더 형식: ### TRACK - 레벨 N - 그룹 M: 그룹명
  토픽 형식: | 토픽 설명 |
---

# Algorithm 튜터 토픽 목록

---

## DS 트랙 — Data Scientist

### DS - 레벨 1 - 그룹 1: 데이터조작

| topic |
|-------|
| 리스트 필터링 + 조건 변환 (list comprehension) |
| set · frozenset 집합 연산 (교집합·합집합·차집합) |
| zip · enumerate 병렬 순회 |
| 딕셔너리 변환 (list → dict) |
| 다중 조건 list comprehension |

### DS - 레벨 1 - 그룹 2: 문자열처리

| topic |
|-------|
| split + join + replace 조합 |
| 문자 빈도 카운팅 (collections.Counter) |
| 대소문자 변환 + 정규화 |
| 부분 문자열 추출 + 슬라이싱 |
| 문자열 → 숫자 변환 + 검증 |

### DS - 레벨 1 - 그룹 3: Hash기초

| topic |
|-------|
| Counter로 빈도 집계 |
| dict로 그룹핑 (key → list) |
| set으로 중복 제거 + 크기 비교 |
| 두 리스트 차집합·교집합 비교 |
| 매핑 테이블 활용 (코드 → 레이블) |

### DS - 레벨 1 - 그룹 4: 리스트슬라이싱

| topic |
|-------|
| 인덱스 범위 슬라이싱 |
| 역순 슬라이싱 ([::-1]) |
| 2D 리스트 행·열 접근 |
| zip으로 두 리스트 병렬 처리 |
| 리스트 분할·병합 (chunk 나누기) |

### DS - 레벨 1 - 그룹 5: 기초집계

| topic |
|-------|
| sum · max · min + 조건 필터 |
| sorted + key 함수 응용 |
| 최빈값 찾기 (Counter.most_common) |
| 범위 내 원소 카운팅 |
| 누적합 없이 단순 집계 비교 |

---

### DS - 레벨 2 - 그룹 1: 정렬·비교

| topic |
|-------|
| 다중 기준 정렬 (key=lambda 튜플) |
| 커스텀 compare (functools.cmp_to_key) |
| 정렬 + 인덱스 추적 (enumerate 후 정렬) |
| 안정 정렬 활용 (동점 처리) |
| 부분 정렬 (heapq.nlargest · nsmallest) |

### DS - 레벨 2 - 그룹 2: Hash심화

| topic |
|-------|
| defaultdict 그룹핑 |
| Counter 집합 연산 (덧셈·뺄셈) |
| 두 dict 비교·병합 |
| 해시로 O(n²) → O(n) 최적화 |
| collections.OrderedDict 삽입 순서 유지 |

### DS - 레벨 2 - 그룹 3: 반복최적화

| topic |
|-------|
| 이중 for loop → Hash로 대체 |
| 중복 제거 후 집계 |
| 슬라이딩 카운터 (윈도우 내 빈도) |
| 사전 정렬 + 이진 탐색 조합 |
| 브루트포스 → 관찰 기반 최적화 |

### DS - 레벨 2 - 그룹 4: 문자열파싱

| topic |
|-------|
| 구분자 다단계 파싱 (CSV 행 처리) |
| 정규식 (re.findall · re.sub · re.match) |
| 토큰화 + 딕셔너리 매핑 |
| 이스케이프·특수문자 처리 |
| 포맷 파싱 (날짜·좌표·로그 형식) |

---

### DS - 레벨 3 - 그룹 1: 시뮬레이션

| topic |
|-------|
| 상태 전이 시뮬레이션 |
| 격자 이동 시뮬레이션 (방향 벡터) |
| 이벤트 기반 처리 (타임스탬프 순서) |
| 명령어 해석 + 실행 |
| 조건부 반복 종료 (while + 복합 조건) |

### DS - 레벨 3 - 그룹 2: 통계·누적합

| topic |
|-------|
| prefix sum 배열 구성 |
| 구간 합 쿼리 (prefix sum 활용) |
| 이동 평균 (슬라이딩 윈도우) |
| 중앙값 유지 (두 힙 패턴) |
| 빈도 기반 통계 (분위수·최빈값) |

### DS - 레벨 3 - 그룹 3: 행렬·2D

| topic |
|-------|
| 2D 배열 탐색 (행·열 순회) |
| 행렬 회전 90도 · 전치 |
| 나선형 순회 |
| 대각선 접근 패턴 |
| 행렬 곱 기초 |

### DS - 레벨 3 - 그룹 4: 우선순위큐

| topic |
|-------|
| heapq 기초 (min-heap push · pop) |
| 최대 힙 (음수 변환 트릭) |
| K번째 원소 추출 |
| 이벤트 스케줄링 (시작·종료 시간) |
| 힙 + 딕셔너리 조합 (top-K with key) |

---

### DS - 레벨 4 - 그룹 1: DP기초

| topic |
|-------|
| 1D DP (피보나치·계단 수 류) |
| 0-1 배낭 문제 |
| 최장 증가 부분수열 (LIS) |
| 동전 교환 (최소 동전 수) |
| 격자 최소 비용 경로 |

### DS - 레벨 4 - 그룹 2: 이진탐색

| topic |
|-------|
| bisect_left · bisect_right 활용 |
| 매개변수 탐색 (parametric search) |
| 정렬 배열에서 범위 카운팅 |
| 이진탐색 + 그리디 조합 |
| 회전 배열 탐색 |

### DS - 레벨 4 - 그룹 3: 그래프BFS

| topic |
|-------|
| BFS 최단 경로 (가중치 없는 그래프) |
| 레벨별 BFS (거리별 처리) |
| 다중 출발점 BFS |
| BFS + 방문 상태 압축 |
| 격자 BFS + 장애물 처리 |

### DS - 레벨 4 - 그룹 4: 분할정복

| topic |
|-------|
| 병합 정렬 응용 |
| 분할정복 최대 부분합 |
| 거듭제곱 빠른 계산 |
| 분할정복 + 메모이제이션 |
| 카운팅 역전 (merge sort 응용) |

---

### DS - 레벨 5 - 그룹 1: DP심화

| topic |
|-------|
| 구간 DP (팰린드롬 분할·행렬 체인) |
| 트리 DP (서브트리 최적값) |
| 비트마스크 DP (방문 상태 압축) |
| DP + 이진탐색 최적화 (LIS O(n log n)) |
| 2D DP + 경로 복원 |

### DS - 레벨 5 - 그룹 2: 대규모최적화

| topic |
|-------|
| 스트리밍 처리 (온라인 알고리즘) |
| 분산 집계 시뮬레이션 |
| 데이터 파이프라인 최적화 |
| 복잡 조건 필터링 최적화 |
| 외부 정렬 시뮬레이션 |

### DS - 레벨 5 - 그룹 3: 복합자료구조

| topic |
|-------|
| 세그먼트 트리 기초 (구간 합 · 구간 최솟값) |
| Trie + 접두사 탐색 |
| Union-Find 응용 (연결 요소 · 사이클) |
| LRU 캐시 (OrderedDict 구현) |
| 균형 BST 시뮬레이션 (SortedList) |

---

## AIE 트랙 — AI Engineer

### AIE - 레벨 1 - 그룹 1: Array·Hash

| topic |
|-------|
| 배열 슬라이딩 카운터 |
| set 중복 검사 (포함 여부 O(1)) |
| 보수(complement) 탐색 패턴 |
| 빈도 맵 구성 + 조회 |
| 두 배열 교집합 · 합집합 |

### AIE - 레벨 1 - 그룹 2: 기본구현

| topic |
|-------|
| 조건부 시뮬레이션 |
| 문자열 변환 + 매핑 |
| 규칙 기반 분류 |
| 순열 없이 조건 탐색 |
| 단순 구현 최적화 |

### AIE - 레벨 1 - 그룹 3: 문자열기초

| topic |
|-------|
| 회문 판별 |
| 아나그램 검사 |
| 문자 압축 (run-length encoding) |
| 문자열 역전 + 단어 재배치 |
| 괄호 짝 기초 (스택 없이 카운터로) |

---

### AIE - 레벨 2 - 그룹 1: Stack·Queue

| topic |
|-------|
| 괄호 유효성 검사 |
| 모노톤 스택 (다음 큰 원소) |
| 큐 기반 순서 처리 (BFS 준비) |
| 스택으로 수식 계산 |
| 연속 최대 사각형 (히스토그램) |

### AIE - 레벨 2 - 그룹 2: 정렬응용

| topic |
|-------|
| 병합 정렬 응용 |
| 정렬 + 두 포인터 조합 |
| 커스텀 비교자 설계 |
| 부분 정렬 + 피벗 전략 |
| 외부 병합 패턴 |

### AIE - 레벨 2 - 그룹 3: Two Pointer·슬라이딩윈도우

| topic |
|-------|
| 투포인터 합 탐색 (Two Sum 변형) |
| 슬라이딩 윈도우 최대 합 (고정 크기) |
| 가변 윈도우 (조건 만족 최소 구간) |
| 투포인터 중복 제거 |
| 연속 부분 배열 조건 카운팅 |

---

### AIE - 레벨 3 - 그룹 1: DFS·BFS

| topic |
|-------|
| DFS 연결 컴포넌트 (재귀) |
| BFS 최단 경로 |
| DFS + 백트래킹 결합 |
| BFS 다중 출발점 |
| 격자 DFS (섬 카운팅) |

### AIE - 레벨 3 - 그룹 2: 백트래킹

| topic |
|-------|
| 순열 생성 |
| 조합 생성 (중복 없음 · 있음) |
| N-Queen |
| 경우의 수 제한 탐색 (가지치기) |
| 스도쿠 풀이 |

### AIE - 레벨 3 - 그룹 3: 그래프탐색

| topic |
|-------|
| 위상 정렬 (Kahn's 알고리즘) |
| 사이클 감지 (DFS 색상 마킹) |
| 강한 연결 요소 기초 (Kosaraju) |
| 이분 그래프 판별 |
| 플로이드-워셜 기초 |

---

### AIE - 레벨 4 - 그룹 1: DP

| topic |
|-------|
| 최장 공통 부분수열 (LCS) |
| 편집 거리 (Edit Distance) |
| 배낭 심화 (부분합 분할) |
| 구간 DP (팰린드롬 분할) |
| 비트마스크 DP (TSP 기초) |

### AIE - 레벨 4 - 그룹 2: Tree·힙

| topic |
|-------|
| 트리 순회 (전위·중위·후위) |
| 최소 신장 트리 (크루스칼) |
| 힙 정렬 |
| 세그먼트 트리 기초 |
| LCA (최소 공통 조상) |

### AIE - 레벨 4 - 그룹 3: 고급그래프

| topic |
|-------|
| 다익스트라 (우선순위 큐) |
| 벨만-포드 (음수 가중치) |
| 위상정렬 + DP 조합 |
| 이분 매칭 기초 |
| 플로우 네트워크 기초 |

---

### AIE - 레벨 5 - 그룹 1: 고급DP

| topic |
|-------|
| 슬라이딩 윈도우 DP |
| CHT (Convex Hull Trick) |
| 확률 DP |
| 트리 DP (재루팅 기법) |
| DP + 세그먼트 트리 최적화 |

### AIE - 레벨 5 - 그룹 2: 세그먼트트리·BIT

| topic |
|-------|
| 구간 합 세그먼트 트리 |
| 구간 업데이트 (Lazy Propagation) |
| BIT (Fenwick Tree) |
| 2D BIT |
| 오프라인 쿼리 처리 |

### AIE - 레벨 5 - 그룹 3: 복합알고리즘

| topic |
|-------|
| 플로우 + 이분 매칭 조합 |
| 문자열 KMP + Aho-Corasick |
| 기하 알고리즘 기초 (볼록 껍질) |
| 랜덤 알고리즘 (퀵셀렉트) |
| 근사 알고리즘 기초 |

---

## 공통 트랙

### 공통 - 레벨 1 - 그룹 1: 기본구현

| topic |
|-------|
| 조건 분기 + 반복 |
| 수 변환 (진법 변환·자릿수 분리) |
| 패턴 생성 + 규칙 탐지 |
| 단순 시뮬레이션 |
| 도형·좌표 기초 |

### 공통 - 레벨 1 - 그룹 2: 문자열

| topic |
|-------|
| 문자 빈도 분석 |
| 부분 문자열 검색 |
| 문자열 치환 + 정규화 |
| 팰린드롬 판별 |
| 아나그램 분류 |

### 공통 - 레벨 1 - 그룹 3: Hash

| topic |
|-------|
| 카운팅 (Counter 기초) |
| 중복 탐지 (set 활용) |
| 매핑 테이블 구성 |
| 그룹핑 (defaultdict) |
| 두 집합 비교 |

---

### 공통 - 레벨 2 - 그룹 1: Stack·Queue

| topic |
|-------|
| 스택 기반 파싱 |
| 큐 기반 순서 처리 |
| 우선순위 큐 기초 |
| 모노톤 스택 |
| 데크 (collections.deque) 활용 |

### 공통 - 레벨 2 - 그룹 2: 정렬

| topic |
|-------|
| 기준 정렬 (단일·다중 key) |
| 안정 정렬 활용 |
| 카운팅 정렬 |
| 정렬 불변성 활용 |
| 부분 배열 정렬 |

### 공통 - 레벨 2 - 그룹 3: 이중반복최적화

| topic |
|-------|
| O(n²) → O(n log n) 개선 |
| 조기 종료 조건 설계 |
| 집합 연산으로 루프 대체 |
| 투포인터로 이중 루프 대체 |
| 캐시(메모이제이션)로 중복 계산 제거 |

---

### 공통 - 레벨 3 - 그룹 1: DFS·BFS

| topic |
|-------|
| DFS 재귀 + 방문 체크 |
| BFS 레벨 탐색 |
| 격자 탐색 (4방향·8방향) |
| 연결 요소 카운팅 |
| 사이클 감지 |

### 공통 - 레벨 3 - 그룹 2: 시뮬레이션

| topic |
|-------|
| 명령어 실행 시뮬레이션 |
| 상태 머신 |
| 격자 이동 + 충돌 처리 |
| 이벤트 처리 (우선순위 기반) |
| 조건 반복 종료 |

### 공통 - 레벨 3 - 그룹 3: 누적합

| topic |
|-------|
| prefix sum 배열 |
| 2D prefix sum |
| 차분 배열 (구간 업데이트) |
| 구간 업데이트 + 조회 |
| 누적합 + 이진탐색 조합 |

---

### 공통 - 레벨 4 - 그룹 1: DP

| topic |
|-------|
| 기초 DP 패턴 (1D 점화식) |
| LIS · LCS |
| 배낭 문제 |
| 경로 최소 비용 |
| 상태 압축 DP |

### 공통 - 레벨 4 - 그룹 2: 이진탐색

| topic |
|-------|
| 정렬 배열 탐색 |
| 매개변수 탐색 (답의 범위 이진탐색) |
| bisect 활용 |
| 이진탐색 + 그리디 조합 |
| 답의 범위 이진탐색 |

### 공통 - 레벨 4 - 그룹 3: 그래프

| topic |
|-------|
| BFS 최단 경로 |
| 다익스트라 |
| 위상 정렬 |
| Union-Find (서로소 집합) |
| 최소 신장 트리 (프림·크루스칼) |

---

### 공통 - 레벨 5 - 그룹 1: 고급DP

| topic |
|-------|
| 구간 DP |
| DP 최적화 (분할정복·CHT) |
| 트리 DP |
| 비트마스크 DP |
| 확률 DP |

### 공통 - 레벨 5 - 그룹 2: 고급그래프

| topic |
|-------|
| 최단경로 심화 (벨만-포드·SPFA) |
| 최대 플로우 |
| 강한 연결 요소 (SCC) |
| 이분 매칭 |
| 트리 분해 (Heavy-Light Decomposition) |

### 공통 - 레벨 5 - 그룹 3: 복합

| topic |
|-------|
| 세그먼트 트리 응용 |
| Trie + 복합 탐색 |
| KMP 문자열 매칭 |
| 다중 알고리즘 조합 |
| 근사 접근 (휴리스틱) |
